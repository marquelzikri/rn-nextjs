{
  "name": "rn-nextjs",
  "private": true,
  "scripts": {
    "android": "lerna --scope=app run android --stream",
    "ios": "lerna --scope=app run ios --stream",
    "webdev": "lerna --scope=web run dev --stream"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**",
      "**@types/react",
      "**@types/react/**"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
